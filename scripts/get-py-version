#!/bin/bash
set -o nounset -o errexit -o pipefail

SCRIPT_ROOT="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
NPM_VERSION="v$($SCRIPT_ROOT/get-version "$@")"

go run "${SCRIPT_ROOT}/get-py-version.go" "${NPM_VERSION}"